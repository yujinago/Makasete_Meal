<div class="container">
  <%= render 'layouts/error', error: @restaurant %>
  <div class="row">
    <h1 class="col-lg-8 text-center mt-5 mx-auto">レビュー編集</h1>
  </div>
  <div class="row">
    <div class="col-lg-4 my-3">
      <div class="text-center">
      <%= image_tag @restaurant.get_restaurant_image(500, 500), size: "300x300", class: "rounded" %>
      </div>
    </div>
    <div class="col-lg-8 my-3 mx-auto">
      <div class="border border-dark p-2 text-center font-weight-bold">
        <p class="my-auto"><%= @restaurant.name %></p>
      </div>
      <div class="border-right border-bottom border-left border-dark">
        <div class="d-md-flex border-bottom">
          <p class="my-auto font-weight-bold">お店URL&emsp;&emsp;:</p>
          <p class="my-auto ml-md-2"><a href="<%= @restaurant.url %>">ホットペッパーグルメで詳細を確認する</a></p>
        </div>
        <div class="d-md-flex border-bottom">
          <p class="my-auto  font-weight-bold">ジャンル名:</p>
          <p class="my-auto  ml-md-2"><%= @restaurant.restaurant_genre.name %></p>
        </div>
        <div class="d-md-flex border-bottom">
          <p class="my-auto font-weight-bold">住所&emsp;&emsp;&emsp;</p>
          <p class="my-auto ml-md-2"><%= @restaurant.address %></p>
        </div>
        <div class="d-md-flex border-bottom">
          <p class="my-auto font-weight-bold">営業時間&emsp;:</p>
          <p class="my-auto ml-md-2"><%= @restaurant.open_time %></p>
        </div>
        <div class="d-md-flex border-bottom">
          <p class="my-auto font-weight-bold">定休日&emsp;&emsp;:</p>
          <p class="my-auto ml-md-2"><%= @restaurant.legular_holiday %></p>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-lg-12 my-3 mx-auto text-center">
      <%= form_with model: @restaurant, url: restaurant_path, method: :patch do |f| %>
        <div class="form-group d-md-flex ">
          <h2><%= f.label :memo, "メモ", style: "margin-right:60px;" %></h2>
          <%= f.text_area :memo, placeholder:"ここにメモすることができます。", size: "60x5", class: "w-75" %>
        </div>
        <%= render 'form_rate', f: f, restaurant: @restaurant %>
        <div class="form-group text-right">
          <%= f.submit "登録する", class: "btn btn-outline-secondary" %>
        </div>
      <% end %>
    </div>
  </div>
</div>